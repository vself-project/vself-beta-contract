{
  "name": "vself-beta-contract",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node contract/compile.js",
    "build:debug": "node contract/compile.js --debug",
    "dev:deploy": "npm run build && near dev-deploy && env-cmd -f ./neardev/dev-account.env node scripts/init.js",  
    "deploy": "./scripts/deploy.testnet.sh"
  },
  "dependencies": {
    "near-api-js": "^0.44.2",
    "near-cli": "^3.1.0",        
    "shelljs": "^0.8.4",    
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1"
  },
  "devDependencies": {        
    "env-cmd": "^10.1.0"
  }
}
